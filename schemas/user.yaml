components:
  schemas:
    User:
      required:
        - id
        - name
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          minLength: 4
          maxLength: 25
        address:
          type: string
